<h1>Listing inspection_details</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @inspection_details.each do |inspection_detail| %>
      <tr>
        <td><%= link_to 'Show', inspection_detail %></td>
        <td><%= link_to 'Edit', edit_inspection_detail_path(inspection_detail) %></td>
        <td><%= link_to 'Destroy', inspection_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Inspection detail', new_inspection_detail_path %>
