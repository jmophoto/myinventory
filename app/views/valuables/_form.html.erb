<%= form_for(@valuable) do |f| %>
  <% if @valuable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@valuable.errors.count, "error") %> prohibited this valuable from being saved:</h2>

      <ul>
      <% @valuable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
